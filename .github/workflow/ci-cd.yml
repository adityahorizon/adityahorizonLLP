name: 🚀 Deploy via SSH

on:
  push:
    branches:
      - main # or whatever branch you deploy from

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3
